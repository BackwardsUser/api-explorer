version: '3.8'

services:
  api-explorer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./content:/app/content
    environment:
      - NODE_ENV=production
    restart: unless-stopped

networks:
  default:
    driver: bridge